---
interface Props {
    tag: string,
}

const { tag } = Astro.props;

const tagForLink = tag.replaceAll(' ', '_');

const link = `/blog/tags/${tagForLink}`;
---
<a class="bg-blue-600 mx-1 px-2 rounded-full" href={link}>
    #{tag}
</a>